service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read;
    }

    match /users/{walletId} {
      allow read, create;
    }

    match /stones/{gemId} {
      allow read, create;
      allow update: if ('auctionIsLive' in request.writeFields)
    }
  }
}
